/**
 * INCONCLUSIVE. A reachable page that simply cannot settle the question either
 * way: no dates, no entries, nothing that maps onto a promise to publish.
 */
export default function Ambiguous() {
  return (
    <main>
      <h1>Under construction</h1>
      <p>
        This site is being rebuilt. Content will return at some point. In the meantime there is
        nothing here to look at.
      </p>
    </main>
  );
}
